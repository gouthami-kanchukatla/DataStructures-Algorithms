class Solution:
    def climbStairs(self, n: int) -> int:
        
               
        prev2 = 0
        prev1  = 1
        
        for  i in range(0, n, 1):
            sum = prev1 + prev2
            prev2  = prev1
            prev1  = sum

            print(sum)

            
        
    return sum
